name: Publish Package

on:
  workflow_dispatch:
    inputs:
      dist_tag:
        description: "npm dist-tag to publish under"
        required: true
        default: latest
        type: choice
        options:
          - latest
          - next

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    uses: clevertask/clevertask-public-workflows/.github/workflows/publish-npm.yml@e4628df244ed07fb722ec9ae9bd512c0db7d0e07
    with:
      dist_tag: ${{ inputs.dist_tag }}
