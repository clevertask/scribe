name: Update pnpm Dependencies

on:
  schedule:
    - cron: "27 10 * * 2"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: weekly-dependency-update
  cancel-in-progress: false

jobs:
  update:
    uses: clevertask/clevertask-public-workflows/.github/workflows/update-pnpm-dependencies.yml@11321374192243f587a0f88fd5fd570099e842e2
    with:
      package_roots: '["."]'
      review_team: dependency-reviewers
    secrets:
      review_token: ${{ secrets.CLEVERTASK_DEPENDENCY_REVIEW_TOKEN }}
